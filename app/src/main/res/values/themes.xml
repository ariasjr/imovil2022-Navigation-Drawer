<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FCRTrainer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.FCRTrainer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FCRTrainer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FCRTrainer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="key">
        <item name="android:layout_width">@dimen/key_size</item>
        <item name="android:layout_height">@dimen/key_size</item>
        <item name="android:textSize">@dimen/score_font</item>
        <item name="android:textColor">@color/key</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily" tools:ignore="NewApi">sans-serif</item>
    </style>

    <style name="key_decimal" parent="@style/key">
        <item name="android:background">@color/key_decimal_background</item>
    </style>

    <style name="key_hexadecimal" parent="@style/key">
        <item name="android:background">@color/key_hexadecimal_background</item>
    </style>

    <style name="key_control" parent="@style/key">
        <item name="android:background">@color/key_control_background</item>
    </style>

    <style name="Title2DarkLight">
        <item name="android:textSize">@dimen/normal_font</item>
        <item name="android:textColor">@color/darkgray</item>
        <item name="android:textStyle">italic</item>
        <item name="android:fontFamily" tools:ignore="NewApi">sans-serif-light</item>
    </style>

    <style name="cards">
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:padding">12dp</item>
    </style>

    <style name="score_info">
        <item name="android:textSize">@dimen/score_font</item>
        <item name="android:textColor">@color/score_info</item>
        <item name="android:fontFamily" tools:ignore="NewApi">sans-serif-light</item>
    </style>

    <style name="game_fragment">
        <item name="android:background">@color/mediumgray</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
    </style>

</resources>